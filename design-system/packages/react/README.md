# @finance-inzicht/react

Thin `@lit/react` adapters for the Web Components in `@finance-inzicht/design-system`. This package adds typed React props and custom events; it contains no separate component implementation.
