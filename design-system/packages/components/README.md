# @finance-inzicht/design-system

Lit Web Components for the Finance Inzicht Design System. Import the package root to register everything, a grouped entry point such as `/button` or `/forms` for selective registration, and `/styles.css` for the optional global foundation.
