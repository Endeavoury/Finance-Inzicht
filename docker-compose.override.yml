services:
  postgres:
    image: postgres:17-alpine
  api:
    healthcheck:
      test: ["CMD-SHELL", "test -r /proc/1/status"]
  worker:
    depends_on:
      api:
        condition: service_healthy
